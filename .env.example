# Copy this file to .env.local and set the value locally. Never commit the real key.
GEMINI_API_KEY=replace-with-your-server-side-gemini-api-key

# Optional. The server defaults to development and port 3000.
NODE_ENV=development
PORT=3000
